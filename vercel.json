{
    "framework": "nextjs",
    "buildCommand": "cd frontend && npm install --include=dev && npm run build",
    "outputDirectory": "frontend/.next",
    "functions": {
        "api/backend.py": {
            "runtime": "python3.12",
            "maxDuration": 60,
            "includeFiles": "backend/layers/**/config.json"
        }
    },
    "rewrites": [
        { "source": "/api/backend/:path*", "destination": "/api/backend" }
    ]
}
